name: generic_outbound
description: Generic outbound correlation (fallback)
priority: 5
enabled: false

detection:
  direction: outbound
  # No specific requirements - acts as fallback

correlation:
  strategy: generic
  force_direction: outbound
  annotations:
    - "Correlator: GenericCorrelator (Outbound)"
  
  notes: |
    Generic fallback for outbound calls that don't match specific use cases.
    Uses the original correlation logic.
